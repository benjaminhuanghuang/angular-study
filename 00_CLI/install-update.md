## Install
```
  nvm use v14

  npm uninstall -g @angular/cli

  npm i -g @angular/cli

  npm i -g @angular/cli@latest

  ng version
```

## Update
```
  npm uninstall -g angular-cli
  
  npm cache clean
  
  npm install -g @angular/cli@latest
```