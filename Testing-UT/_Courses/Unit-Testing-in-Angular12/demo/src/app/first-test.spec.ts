//
describe("My first test", () => {
  let sut;

  beforeEach(() => {
    sut = {}
  })

  it("", () => {
    // arrange
    sut.a = false;
    // act

    // assert
    expect(sut.a).toBe(false);
  })
})